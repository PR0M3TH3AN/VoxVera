{
  "name": "voxvera-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Cross-browser end-to-end tests for the VoxVera static Nostr flyer client.",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
